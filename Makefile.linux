dlls:
	cd dll_src && make -f Makefile.linux

clean:
	$(RM) ext/haxelib.xml
	$(RM) *.n

clean-kate:
	find . -name "*~" -exec rm {} \;

clean-neko:
	find . -name "*.n" -exec rm {} \;

distclean: clean clean-kate clean-neko

docs:
	cd ext && haxe all.hxml
	cd ext/Doc && haxedoc "../flash.xml;flash\" \"../neko.xml;neko\" \"../js.xml;js"
	cd ext && $(RM) *.swf *.n *.js *.xml

#	cd ext && mv index.html Doc/ && mv content Doc/
